# Attendance Backend

Python-based attendance backend for face recognition and attendance tracking.

## Setup

1. Create and activate a Python virtual environment.
2. Install dependencies from `requirements.txt` if available.
3. Run `python main.py`.

## Notes

- Ignore `venv` and database files in Git.
- This repository is already configured with a remote on GitHub.
